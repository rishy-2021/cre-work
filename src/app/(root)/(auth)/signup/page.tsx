import SignUpForm from "@/components/ui/sign-up-form";

const Login = () => {

  return (
    <div className="flex flex-1 min-h-screen items-center justify-center" style={{background:"linear-gradient(180deg, #FFFFFF 0%, #AFA3FF 100%)"}}>
      <SignUpForm />
    </div>
  );
};

export default Login;
